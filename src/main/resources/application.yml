spring:
  application:
    name: job-application-service

  datasource:
    url: jdbc:h2:mem:jobpostsdb
    driver-class-name: org.h2.Driver
    username: test123
    password: test#123

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console

  logging:
    level:
      org.springframework.security: DEBUG


server:
  port: 8085